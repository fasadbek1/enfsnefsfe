pull_request_rules:
  - name: h1 review bot_account permission probe
    conditions:
      - base=main
      - -closed
    actions:
      review:
        type: APPROVE
        bot_account: h1-disco07-b
