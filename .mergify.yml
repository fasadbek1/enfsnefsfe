pull_request_rules:
  - name: h1 impersonate comment
    conditions:
      - base=main
      - -closed
    actions:
      comment:
        message: "h1-disco07 authorization test marker"
        bot_account: h1-disco07-b
