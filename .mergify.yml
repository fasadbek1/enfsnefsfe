pull_request_rules:
  - name: ARM-A merge with bot_account
    conditions:
      - base=main
      - -closed
    actions:
      merge:
        merge_bot_account: h1-disco07-b
  - name: ARM-B comment with bot_account
    conditions:
      - base=main
      - -closed
    actions:
      comment:
        message: "h1-disco07 side-by-side differential marker"
        bot_account: h1-disco07-b
